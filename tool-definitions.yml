- type: function
  function:
    name: getDataBaseSchema
    description: Get Schema definition for all tables in the database
    parameters:
      type: object
      properties: {}
        
- type: function
  function:
    name: query
    description: execute the sql query
    parameters:
      type: object
      properties:
        sql:
          type: string
          description: the sql query
      required:
        - sql